<h4>Projects</h4>
<hr>
<% projects.each(function (project) { %>
  <div class="project-row row" project-id="<%= project.escape('id') %>">
    <a class="project-link col-md-2" style="float:none;" project-id="<%= project.escape('id') %>" href="#"><%= project.escape('name') %></a>
  </div>
<% }) %>

<br>
<a href="#" class="create-project">Create project</a>

<script>
  $('.project-row').hover(
    function () {
      var deleteButton ="<a class='delete-project text-danger pull-right' project-id='"
                       + BC.getID(this, 'project')
                       + "' href='#'>Delete</a>";
      $(this).append($(deleteButton));
    },
    function () {
      $(this).find('.delete-project').remove();
    });
</script>